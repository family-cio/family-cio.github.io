## Share wedding photos

The site created is located on [GitHub](https://family-cio.github.io/)

### Design decisions:
**Github to host the page (HTML, JS, CSS) ~~and contain the photos (in the repo)~~**
- I separated out the CSS and the JS
- I uploaded the full code (all 4 versions) to Google Drive. Everything all on the root (fight me)
- Second big mistake: Host the photos in a DIFFERENT repo. Every time a photo was posted, that triggered a website deployment

**Cloudflare to host a JS worker service**
- First big mistake: I tried to have the JS in GitHub call the GitHub API, but GitHub doesn't allow the PAT to be stored in code (valid)
- So I created a Cloudflare Worker that holds the various GitHub values. The JS here just forwards everything to CF
- 100k free daily calls (just for my reference)

**Second repo for photos**
- The photos are posted to a separate repo
- The JS for the worker is also saved in that repo

---
*Code generated by Claude, debugged by me*
